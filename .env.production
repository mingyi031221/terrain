# Production build: the frontend is served same-origin by the Node service,
# so the API base must be relative (empty). This overrides the dev value in
# .env during `vite build`. Safe to commit — contains no secrets.
VITE_API_BASE_URL=
